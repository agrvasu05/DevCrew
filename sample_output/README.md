# Sample output

⚠️ Placeholder folder. Do ONE real run:

    python main.py "Build a CLI expense tracker with categories and monthly totals"

then copy the generated `output/` files here and commit them:

    cp output/* sample_output/ && rm sample_output/README.md

Committing a real run makes the repo credible — visitors see actual results
without spending API credits.
